import React from "react";
import styled from "styled-components";

interface Props {}

const LogInWrapper = styled.div``;

const LogIn: React.FC<Props> = () => {
  return <LogInWrapper>logic</LogInWrapper>;
};

export default LogIn;
