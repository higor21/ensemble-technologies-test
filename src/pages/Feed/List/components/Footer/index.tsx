import React from "react";
import styled from "styled-components";

interface Props {}

const FooterWrapper = styled.div``;

const Footer: React.FC<Props> = () => {
  return <FooterWrapper>logic</FooterWrapper>;
};

export default Footer;
