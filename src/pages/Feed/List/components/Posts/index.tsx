import React from "react";
import styled from "styled-components";

interface Props {}

const PostsWrapper = styled.div``;

const Posts: React.FC<Props> = () => {
  return <PostsWrapper>logic</PostsWrapper>;
};

export default Posts;
